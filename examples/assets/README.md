This folder is for example/static assets that clutter the repository root.

Guidance:
- Move example PDFs or images used only for demos into this folder, for example:
  - `examples/assets/sample.pdf`
  - `examples/assets/agent_history.gif`

If you want to permanently remove the sample files from the repository root, run the cleanup script in the `scripts/` directory.
